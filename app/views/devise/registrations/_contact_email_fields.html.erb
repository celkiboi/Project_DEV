<div class="nested-fields">
  <%= f.text_field :email, placeholder: "Email" %>
  <%= f.hidden_field :_destroy %>
  <button type="button" data-action="nested-form#remove"><%= t('devise.registrations.edit.remove') %></button>
</div>