<!DOCTYPE html>
<html>
  <head>
    <title>Project DEV</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-sans leading-normal tracking-normal bg-white text-gray-900 dark:bg-gray-900 dark:text-white transition-colors duration-300">
    <header class="shadow-md">
      <%= render "shared/nav" %>
    </header>

    <div class="p-4 sm:p-6 lg:p-8">
      <%= yield %>
    </div>
  </body>
</html>
