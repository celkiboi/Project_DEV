<!DOCTYPE html> 
<html lang="en">
  <head>
    <title>Admin Panel</title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-white">

    <div class="min-h-screen flex flex-col">
      
      <nav class="w-full bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
        <div class="container mx-auto p-4 flex justify-between items-center">
          <h2 class="text-xl font-semibold text-blue-500 dark:text-blue-300"><%= t('views.layouts.admin.title') %></h2>

          <ul class="flex gap-x-6">
            <li><%= link_to t('views.layouts.admin.administrators'), admin_admins_path, class: "text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold" %></li>
            <li><%= link_to t('views.layouts.admin.users'), admin_users_path, class: "text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold" %></li>
            <li><%= link_to t('views.layouts.admin.posts'), admin_posts_path, class: "text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold" %></li>
            <%= button_to t('views.layouts.admin.logout'), 
              destroy_admin_session_path, 
              method: :delete,
              class: "text-red-500 hover:text-red-700 dark:text-red-300 dark:hover:text-red-500 text-lg font-semibold" %>
          </ul>
        </div>
      </nav>

      <div class="flex-1 p-6">
        <%= yield %>
      </div>

    </div>

  </body>
</html>
