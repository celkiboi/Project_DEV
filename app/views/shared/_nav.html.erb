<nav class="container mx-auto p-4 flex justify-between items-center bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
  <a href="<%= root_path %>" class="text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold">
    <%= t('views.shared.nav.home') %>
  </a>

  <% if user_signed_in? %>
    <a href="<%= user_profile_path(current_user) %>" class="text-purple-500 hover:text-purple-700 dark:text-purple-300 dark:hover:text-purple-500 text-lg font-semibold">
      <%= t('views.shared.nav.profile') %>
    </a>
    <a href="<%= edit_user_registration_path %>" class="text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold">
      <%= t('views.shared.nav.edit') %>
    </a>
    <%= link_to t('views.shared.nav.logout'), destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "text-red-500 hover:text-red-700 dark:text-red-300 dark:hover:text-red-500 text-lg font-semibold" %>
    <%= link_to t('views.shared.nav.new-post'), post_new_path, class: "text-green-500 hover:text-green-700 dark:text-green-300 dark:hover:text-green-500 text-lg font-semibold" %>
  <% else %>
    <a href="<%= new_user_session_path %>" class="text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-500 text-lg font-semibold">
      <%= t('views.shared.nav.login') %>
    </a>
  <% end %>
</nav>
